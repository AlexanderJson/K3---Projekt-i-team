


- Sortera ut alla state calls
- implementera service calls istället
- lägg in bra filtering, indexering
- lös flytt funktioner med service calls (DOM hämtar kopia från en sorted array istället som innehåller
  den renderade datan + overflow)

- Skapa eventbuss 
- Bygg in virtualization listan igen
- main div = main
- appendChild vid rerender istället för inner.html
- hitta minnesleaks (t.ex nodes som skapas osv.)
- lägg in seed funktionen igen

- lägg in csv och json readers igen 




låg prio:
- fixa layout med grid area 
- hitta sätt att mäta prestanda? 
- kolla upp normalisering + diff checks med för just detta 